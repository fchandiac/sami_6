module.exports = {
    presets: [
      '@babel/preset-env', // Transpila el código a una versión compatible con los navegadores actuales
      '@babel/preset-react' // Transpila el código JSX en JavaScript estándar (solo si estás utilizando React)
    ],
    plugins: [
      // Aquí puedes agregar plugins adicionales si es necesario
    ]
  };
  