1.- Agregar la definicion de de la sala de  venta en el archivo config.json
    utilizando en congig.js (page), un storages.findAll() y alacenando el ID, 
    luego en la carga del Layout, se carga el ID al estado global de la aplicacion.